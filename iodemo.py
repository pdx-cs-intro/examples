# Ask the user to type something, with prompt "? ". Turn the
# typed input into a float number.
x = float(input("? "))
# Add 5 to the user's number and store the result in variable y.
y = x + 5
# Show the user's number and the sum.
print(x, "+ 5 =", y)
